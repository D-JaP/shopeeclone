spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/product_db
        username: postgres
        password: root

    data:
        rest:
            base-path: /api/v1

    jpa:
        show-sql: true

rsa:
    public-key: classpath:certs/public.pem

server:
    port: 8081